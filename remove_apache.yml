- host: all
- become: true
  tasks:
    -name: update repository index
      apt:
        update_cache: no

    -name: install apache2
      apt:
        name: apache2
        state: absent

    -name: start apache2 service
      service:
        name: apache2
        state: absent    